/**
 * Represents an interface for generating random IDs.
 */
export interface IRandom {
  /**
   * Generates a random ID.
   * @returns A string representing the random ID.
   */
  randomID(): string;
}
